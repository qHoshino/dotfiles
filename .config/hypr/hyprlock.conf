# sample hyprlock.conf
# for more configuration options, refer https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock
#
# rendered text in all widgets supports pango markup (e.g. <b> or <i> tags)
# ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#general-remarks
#
# shortcuts to clear password buffer: ESC, Ctrl+U, Ctrl+Backspace
#
# you can get started by copying this config to ~/.config/hypr/hyprlock.conf
#

$font = JetBrainsMonoNL Nerd Font Mono

general {
    hide_cursor = false
}

# uncomment to enable fingerprint authentication
auth {
    fingerprint {
        enabled = true
        ready_message = Scan fingerprint to unlock
        present_message = Scanning...
        retry_delay = 250 # in milliseconds
    }
}

animations {
    enabled = true
    bezier = linear, 1, 1, 0, 0
    animation = fadeIn, 1, 5, linear
    animation = fadeOut, 1, 5, linear
    animation = inputFieldDots, 1, 2, linear
}

background {
    monitor =
    path = screenshot
    blur_passes = 3
}

input-field {
    monitor =
    size = 20%, 5%
    outline_thickness = 0

    inner_color = rgba(113, 81, 210, 0.1)
    outer_color = rgba(550099ee) rgba(000099ee) 45deg
    check_color = rgba(113, 81, 210, 0.2)
    fail_color = rgba(113, 81, 210, 0.2)

    font_color = rgba(226, 198, 254, 1)
    fade_on_empty = false
    rounding = 5

    font_family = $font
    placeholder_text = Insert Kremówka
    fail_text = You fucked up.

    # uncomment to use a letter instead of a dot to indicate the typed password
    # dots_text_format = *
    # dots_size = 0.4
    dots_spacing = 0.3

    # uncomment to use an input indicator that does not show the password length (similar to swaylock's input indicator)
    # hide_input = true

    position = 0, -20
    halign = center
    valign = center
}

# TIME
label {
    monitor =
    text = $TIME # ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#variable-substitution
    font_size = 90
    font_family = $font

    position = 30, 0
    halign = left
    valign = top
    color = rgba(226, 198, 254, 1)
}

# DATE
label {
    monitor =
    text = cmd[update:60000] date +"%A, %d %B %Y" # update every 60 seconds
    font_size = 25
    font_family = $font

    position = 30, -150
    halign = left
    valign = top
    color = rgba(226, 198, 254, 1)
}
label {
    monitor = 
    text = Umazing text lies here, wei

    font_size = 12

    position = 20, 20 
    halign = left
    valign = bottom
    color = rgba(226, 198, 254, 1)
    rotate = -10
}
image {
    monitor =
    path = ~/Pictures/Arch.png
    rounding = 0
    border_size = 0
    halign = right
    valign = top
    size = 200
    position = -20, -20

}
label {
    monitor =
    text = Btw
    font_size = 30
    font_family = JetBrainsMonoNL Nerd Font Mono Bold

    position = -20, -175
    halign = right
    valign = top
    color = rgba(226, 198, 255, 1)
    rotate = 10
}

# label {
#     monitor =
#     text = $LAYOUT[en,ru]
#     font_size = 24
#     onclick = hyprctl switchxkblayout all next
# 
#     position = 250, -20
#     halign = center
#     valign = center
#     color = rgba(226, 198, 254, 1)
# }
